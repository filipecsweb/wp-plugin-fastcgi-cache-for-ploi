{
    "name": "ploi-fastcgi-cache",
    "version": "1.0.0",
    "description": "Admin UI (Alpine + Tailwind v4) and E2E tooling for the Ploi FastCGI Cache plugin.",
    "license": "GPL-2.0-or-later",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "watch": "vite build --watch",
        "e2e": "playwright test",
        "e2e:install": "playwright install --with-deps"
    },
    "dependencies": {
        "alpinejs": "^3.14.1"
    },
    "devDependencies": {
        "@playwright/test": "^1.45.0",
        "@tailwindcss/vite": "^4.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
